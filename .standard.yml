ruby_version: 3.3
ignore:
  - 'db/migrations/**/*'
  - 'vendor/**/*'
  - 'packaging/homebrew/**/*'  # Homebrew DSL — Pathname `/` 연산자 관례 (표준 Ruby 와 다름)
  - 'dist/**/*'                # macOS DMG 빌드 산출물 (build.sh 가 만든 .app 번들 — 소스 복사본)
